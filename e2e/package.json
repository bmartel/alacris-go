{
  "name": "alacris-go-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Browser tests for the claims the Go tests cannot reach.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browser": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
